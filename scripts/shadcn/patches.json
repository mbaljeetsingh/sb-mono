[
  {
    "file": "separator/Separator.vue",
    "find": "'bg-border shrink-0 data-[orientation=horizontal]:h-px data-[orientation=horizontal]:w-full data-[orientation=vertical]:h-full data-[orientation=vertical]:w-px',\n        props.class,",
    "replace": "'bg-border shrink-0',\n        props.orientation === 'vertical' ? 'w-px h-full' : 'h-px w-full',\n        props.class,",
    "reason": "Revert to JS conditional — Tailwind v4 does not generate data-attribute variant CSS from layer files"
  },
  {
    "file": "button/index.ts",
    "find": "\"border bg-background shadow-xs hover:bg-accent hover:text-accent-foreground dark:bg-input/30 dark:border-input dark:hover:bg-input/50\"",
    "replace": "\"border border-input hover:bg-accent hover:text-accent-foreground dark:bg-input/30 dark:hover:bg-input/50\"",
    "reason": "Clean outline button: point at --input (both modes) and drop bg-background + shadow-xs so buttons integrate with tinted surfaces instead of forming a solid island"
  },
  {
    "file": "sheet/SheetContent.vue",
    "find": "class=\"ring-offset-background focus:ring-ring data-[state=open]:bg-secondary absolute top-4 right-4 rounded-xs opacity-70 transition-opacity hover:opacity-100 focus:ring-2 focus:ring-offset-2 focus:outline-hidden disabled:pointer-events-none\"",
    "replace": "class=\"ring-offset-background focus:ring-ring data-[state=open]:bg-secondary absolute rounded-xs opacity-70 transition-opacity hover:opacity-100 focus:ring-2 focus:ring-offset-2 focus:outline-hidden disabled:pointer-events-none\"\n        :style=\"{ top: (side === 'right' || side === 'left') ? 'max(1rem, env(safe-area-inset-top))' : '1rem', right: side === 'right' ? 'max(1rem, env(safe-area-inset-right))' : '1rem', left: side === 'left' ? 'max(1rem, env(safe-area-inset-left))' : undefined }\"",
    "reason": "iOS safe area: close button needs safe-area offset on side sheets (right/left) but fixed 1rem on top/bottom sheets to avoid PWA status bar offset"
  },
  {
    "file": "progress/Progress.vue",
    "find": "  <ProgressRoot\n    :model-value=\"modelValue\"\n    v-bind=\"delegatedProps\"",
    "replace": "  <ProgressRoot\n    v-bind=\"delegatedProps\"",
    "reason": "delegatedProps already contains modelValue (only `class` is stripped), so the explicit :model-value binding is a duplicate — vue-tsc TS2783"
  },
  {
    "file": "toggle/index.ts",
    "find": "data-[state=on]:bg-accent data-[state=on]:text-accent-foreground",
    "replace": "data-[state=on]:bg-primary/10 data-[state=on]:text-foreground data-[state=on]:border-primary data-[state=on]:ring-1 data-[state=on]:ring-primary",
    "reason": "Selected state was invisible: --accent equals --border in both modes, so data-[state=on]:bg-accent gave a 1.34:1 fill delta in dark / 1.16:1 in light with identical text colour — WCAG 1.4.11 needs 3:1 for a state indicator. A primary-coloured ring carries the state (7.3:1 dark, 5.9:1 light) with the tint as reinforcement"
  },
  {
    "file": "toggle-group/ToggleGroupItem.vue",
    "find": "import type { toggleVariants } from '@sb/layer-ui/components/ui/toggle'",
    "replace": "import { toggleVariants } from '@sb/layer-ui/components/ui/toggle'",
    "reason": "toggleVariants is CALLED in the template, so the import must be a value import — `import type` is erased and vue-tsc errors (TS2339)"
  },
  {
    "file": "input/Input.vue",
    "find": "const emits = defineEmits<{\n  (e: \"update:modelValue\", payload: string | number): void\n}>()",
    "replace": "const emits = defineEmits<(e: \"update:modelValue\", payload: string | number) => void>()",
    "reason": "biome lint/style/useShorthandFunctionType — keep the generated file in the shape the repo linter enforces"
  }
]

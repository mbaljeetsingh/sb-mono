node_modules
dist
.output
.nuxt
.nitro
.cache
*.log
.DS_Store
.env
.env.local
.env.*.local
coverage
.turbo
.vercel
.netlify
.playwright-mcp
.tmp/

# Stray shadcn-CLI output — the CLI sometimes resolves the @sb/ alias as a
# literal directory. Real components live in layers/ui/components/ui/.
/@sb/

# UI redesign exploration — palette candidates, per-page audit, and the final
# spec (mockups/FINAL.md). Local working notes, not shipped source.
mockups/

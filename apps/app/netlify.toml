[build]
  ignore = "bash scripts/netlify-ignore.sh app"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback is auto-handled by Nuxt's generated 200.html — no redirect needed.

[[headers]]
  for = "/*"
  [headers.values]
    Permissions-Policy = "wake-lock=*, fullscreen=*"
